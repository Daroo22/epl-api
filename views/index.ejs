<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home Page</title>
    <link rel="stylesheet" href="/css/index.css" /> <!-- Adjust the path if needed -->
  </head>
  <body>
    <% if (user) { %>
      <h1>Hello, <%= user.username %></h1>
      <p>
        <a href="/auth/sign-out">Sign out</a>
      </p>
      <button class="favorites-button">
        ❤️
        <span class="favorites-label">Favorites</span>
    </button>
    
    <% } else { %>
      <h1>Welcome to the Soccer Hub.</h1>
      <h2>Don't have an account?</h2>
      <a href="/auth/sign-up">Sign up</a>
      <a href="/auth/sign-in">Sign in</a>
    <% } %>
    
    <form action="/search" method="GET">
      <input type="text" name="player" placeholder="Enter player's name" required>
      <button type="submit">Find Player!</button>
    </form>
    
      
      <% if (user) { %>
        <a href="/players">See All Players</a>
      <% } %>
    </form>
  </body>
</html>
